<h1>Editing equipment type</h1>

<% form_for([:admin, @equipment_type]) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', [:admin, @equipment_type] %> |
<%= link_to 'Back', admin_equipment_types_path %>