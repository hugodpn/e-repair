<h1><%= t('reason_fault.create.title') %></h1>

<% form_for([:admin, @reason_fault]) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label t('reason_fault.model.name') %><br />
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.submit t('general.create') %>
  </p>
<% end %>

<%= link_to t('general.back'), admin_reason_faults_path %>